// 判断数组中是否包含某个值
function includes(arr, value) {
  if (!arr || arr.length === 0) return false;
  for (var i = 0; i < arr.length; i++) {
    if (arr[i] === value) {
      return true;
    }
  }
  return false;
}

module.exports = {
  includes: includes
};


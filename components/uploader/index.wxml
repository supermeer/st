<view class="uploader">
  <t-upload
    media-type="{{mediaType}}"
    files="{{files}}"
    max="{{max}}"
    gridConfig="{{gridConfig}}"
    sizeLimit="{{sizeLimit}}"
    bind:add="onAdd"
    bind:remove="onRemove"
    bind:click="onClick"
  >
    <view slot="add-content" class="upload-btn-wrap">
      <t-image class="upload-btn" src="/static/contentadd/upload_icon.png" />
    </view>
  </t-upload>
</view>
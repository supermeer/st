.chat-container {
  width: 100%;
  /* background: linear-gradient(345deg, #EEF2FF 4%, #CCF9FF 69%, #FFB79B 96%); */
  /* background: #00000069; */
  background: #00000049;
  display: flex;
  flex-direction: column;
  box-sizing: border-box;
  /* 匀速过渡 */
  transition: height 0.2s ease-in-out;
}

.role-info-container {
  width: 100%;
  height: 100rpx;
  padding: 0 32rpx;
  box-sizing: border-box;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: flex-start;
}

.role-info {
  box-sizing: border-box;
  /* width: 260rpx; */
  height: 70rpx;
  color: #fff;
  font-size: 28rpx;
  background: #00000069;
  border-radius: 35rpx;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 0 24rpx;
  transition: transform 0.2s ease, background-color 0.2s ease;
}

.role-info:active {
  transform: scale(0.96);
  background: #00000099;
}

.role-avatar {
  width: 40rpx;
  height: 40rpx;
  border-radius: 50%;
  overflow: hidden;
  flex-shrink: 0;
}

.role-msg {
  display: flex;
  flex-direction: column;
  justify-content: center;
  margin-left: 16rpx;
}

.role-name {
  font-size: 24rpx;
  font-weight: bold;
}

.role-job {
  font-size: 20rpx;
  /* 不换行，超出省略号 */
  width: 100%;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
  text-overflow: ellipsis;
}

.message-list {
  flex: 1;
  height: 0; /* 配合 flex: 1 使用，确保有明确高度 */
  /* padding: 0 24rpx; */
  box-sizing: border-box;
  position: relative;
}

.message-list.scrolled-up {
  -webkit-mask-image: linear-gradient(
    to bottom,
    transparent 0%,
    transparent 30%,
    rgba(0, 0, 0, 1) 40%,
    rgba(0, 0, 0, 1) 100%
  );
  mask-image: linear-gradient(
    to bottom,
    transparent 0%,
    transparent 30%,
    rgba(0, 0, 0, 1) 40%,
    rgba(0, 0, 0, 1) 100%
  );
}

.plot-box {
  max-width: calc(100% - 48rpx);
  margin: 24rpx 0 24rpx 24rpx;
  background-color: rgba(0, 0, 0, 0.3);
  border-radius: 24rpx;
  padding: 24rpx 32rpx;
  color: var(--main-color-white);
  border-radius: 24rpx;
  font-size: 24rpx;
  position: relative;
  padding-bottom: 56rpx; /* 给右下角按钮留出空间 */
  /* 卡片展开收起过渡 */
  overflow: hidden;
  transition: max-height 0.35s cubic-bezier(0.4, 0, 0.2, 1), padding 0.3s ease;
}

.plot-title {
  display: flex;
  align-items: center;
  font-size: 28rpx;
  font-weight: bold;
  margin-bottom: 12rpx;
}

.plot-icon {
  width: 32rpx;
  height: 32rpx;
  margin-right: 12rpx;
}

.plot-content {
  line-height: 1.8;
  letter-spacing: 0.8rpx;
  display: block;
  /* 内容展开收起过渡 */
  transition: max-height 0.35s cubic-bezier(0.4, 0, 0.2, 1), opacity 0.25s ease;
  overflow: hidden;
}

.plot-box .fold-toggle {
  position: absolute;
  right: 16rpx;
  bottom: 12rpx;
  font-size: 24rpx;
  color: #ddd;
  padding: 8rpx 16rpx;
  transition: transform 0.2s ease, color 0.2s ease;
}

.plot-box .fold-toggle:active {
  transform: scale(0.95);
  color: #fff;
}

.message-item {
  margin-bottom: 24rpx;
  animation: messageSlideIn 0.35s cubic-bezier(0.4, 0, 0.2, 1);
  /* 消息项位置变化过渡 */
  transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1), margin 0.3s ease;
}

@keyframes messageSlideIn {
  from {
    opacity: 0;
    transform: translateY(20rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.icon-btn {
  margin-right: 8rpx;
  color: #aaa;
}

.retry-container {
  margin-bottom: 24rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #aaa;
  font-size: 24rpx;
  animation: retryFadeIn 0.4s ease;
}

@keyframes retryFadeIn {
  from {
    opacity: 0;
    transform: scale(0.95);
  }
  to {
    opacity: 1;
    transform: scale(1);
  }
}

.retry-text {
  color: var(--theme-color);
  font-size: 24rpx;
  text-decoration: underline;
  margin-left: 12rpx;
  transition: opacity 0.2s ease;
}

.retry-text:active {
  opacity: 0.7;
}

.input-box {
  flex: none;  /* 等同于 flex: 0 0 auto，不增长也不收缩 */
}

.loading-more-tip {
  position: absolute;
  top: -50px;
  left: 0;
  right: 0;
  text-align: center;
  color: #fff;
  font-size: 24rpx;
  height: 50px;
  display: flex;
  align-items: center;
  justify-content: center;
}

.ai-tip {
  color: #aaa;
  text-align: center;
  display: inline-block;
  font-size: 24rpx;
}

.tip-content {
  display: flex;
  align-items: center;
  justify-content: center;
  opacity: 0.8;
  transition: opacity 0.25s ease, transform 0.25s ease;
  animation: tipFadeIn 0.3s ease;
}

@keyframes tipFadeIn {
  from {
    opacity: 0;
    transform: translateY(-10px);
  }
  to {
    opacity: 0.8;
    transform: translateY(0);
  }
}

.tip-icon {
  font-size: 32rpx;
  margin-right: 12rpx;
  transition: transform 0.3s ease;
}

.tip-icon.release {
  transform: rotate(180deg);
}

.tip-text {
  font-size: 26rpx;
  line-height: 1.5;
}

.loading-icon {
  width: 32rpx;
  height: 32rpx;
  border: 4rpx solid rgba(255, 255, 255, 0.3);
  border-top-color: #fff;
  border-radius: 50%;
  margin-right: 12rpx;
  animation: rotate 0.8s linear infinite;
}

@keyframes rotate {
  0% {
    transform: rotate(0deg);
  }
  100% {
    transform: rotate(360deg);
  }
}

/* 全局蒙版样式 */
.global-action-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.6);
  z-index: 10000;
  animation: maskFadeIn 0.25s cubic-bezier(0.4, 0, 0.2, 1);
}

@keyframes maskFadeIn {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

/* 按钮弹窗样式 */
.global-action-buttons {
  display: flex;
  gap: 16rpx;
  padding: 16rpx;
  background: rgba(40, 40, 40, 0.95);
  -webkit-backdrop-filter: blur(20px);
  backdrop-filter: blur(20px);
  border-radius: 24rpx;
  box-shadow: 0 8rpx 32rpx rgba(0, 0, 0, 0.3);
  transform: scale(0.8) translateY(-10px);
  opacity: 0;
  animation: buttonPopIn 0.3s cubic-bezier(0.34, 1.56, 0.64, 1) forwards;
}

.global-action-buttons.buttons-show {
  transform: scale(1) translateY(0);
  opacity: 1;
}

@keyframes buttonPopIn {
  0% {
    transform: scale(0.8) translateY(-10px);
    opacity: 0;
  }
  100% {
    transform: scale(1) translateY(0);
    opacity: 1;
  }
}

.action-btn {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 8rpx;
  min-width: 120rpx;
  padding: 20rpx 24rpx;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.15), rgba(255, 255, 255, 0.05));
  border-radius: 20rpx;
  color: #fff;
  font-size: 24rpx;
  transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
  border: 1px solid rgba(255, 255, 255, 0.1);
}

.action-btn:active {
  transform: scale(0.95);
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.25), rgba(255, 255, 255, 0.1));
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.2);
}

.action-icon {
  width: 48rpx;
  height: 48rpx;
  transition: transform 0.2s cubic-bezier(0.4, 0, 0.2, 1);
}

.action-btn:active .action-icon {
  transform: scale(1.1);
}

/* 蒙版关闭动画 */
.global-action-mask.mask-closing {
  animation: maskFadeOut 0.2s cubic-bezier(0.4, 0, 0.2, 1) forwards;
}

@keyframes maskFadeOut {
  from {
    opacity: 1;
  }
  to {
    opacity: 0;
  }
}

/* 按钮关闭动画 */
.global-action-buttons.buttons-closing {
  animation: buttonPopOut 0.2s cubic-bezier(0.4, 0, 0.2, 1) forwards;
}

@keyframes buttonPopOut {
  from {
    transform: scale(1) translateY(0);
    opacity: 1;
  }
  to {
    transform: scale(0.85) translateY(-8px);
    opacity: 0;
  }
}